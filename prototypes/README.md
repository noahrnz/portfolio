# Interactive prototypes

- **climbing-gym** — Touchstone app (Figma Make export). To rebuild after editing: `cd climbing-gym-src && pnpm install && pnpm run build`, then copy `dist/*` into `public/prototypes/climbing-gym/`.
